apiVersion: v1
kind: Pod
metadata:
  name: mysql-configmap-pod
spec:
  containers:
  - name: mysql
    image: mysql:8.0
    envFrom:
    - configMapRef:
        name: mysql-config
    env:
    - name: MYSQL_ROOT_PASSWORD
      value: rootpass123
    ports:
    - containerPort: 3306
